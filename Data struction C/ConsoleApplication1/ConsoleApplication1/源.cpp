# include "Search Table.h"

int main() {
	/*
	BSTree T;
	InitBST(T);
	SSTable S;
	CreateSST(S);
	bool taller = FALSE;
	for (int i = 1; i <= S.length; ++i)
		InsertAVL(T,S.elem[i],taller);
	InOrderTraverse(T,MyPrint);
	DeleteBST(T, 'g');
	InOrderTraverse(T,MyPrint);
	DeleteBST(T, 'd');
	InOrderTraverse(T,MyPrint);
	DestroySST(S);
	DestroyBST(T);
	*/
	int a[3] = { 0 };
	int* p = a;
	p++;
	printf("%d", *p);
}